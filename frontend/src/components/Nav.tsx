// src/components/Header.tsx
import React from 'react';
import { StyledNav } from '@components/Nav/StyledNav';

const Nav: React.FC = () => {
  return (
    <StyledNav>
      <h1>Nav</h1>
    </StyledNav>
  );
};

export default Nav;